# anytrain.example

Small package resources for tests, examples, and smoke checks.

These files are bundled with the package so examples can read real audio without
depending on external datasets, private paths, downloads, or pretrained models.
